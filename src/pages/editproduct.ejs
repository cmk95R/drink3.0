<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Editar Producto</title>
</head>
<body>
  <h1>Editar Producto</h1>
  <form action="/products/edit/<%= product._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
    <label>Nombre: <input type="text" name="name" value="<%= product.name %>" required /></label><br/>
    <label>Precio: <input type="number" step="0.01" name="price" value="<%= product.price %>" required /></label><br/>
    <label>Stock: <input type="number" name="stock" value="<%= product.stock %>" required /></label><br/>
    <label>Categoría:
      <select name="category" required>
        <option value="vino" <%= product.category==='vino' ? 'selected' : '' %>>Vino</option>
        <option value="cerveza" <%= product.category==='cerveza' ? 'selected' : '' %>>Cerveza</option>
        <option value="whisky" <%= product.category==='whisky' ? 'selected' : '' %>>Whisky</option>
        <option value="vodka" <%= product.category==='vodka' ? 'selected' : '' %>>Vodka</option>
        <option value="otros" <%= product.category==='otros' ? 'selected' : '' %>>Otros</option>
      </select>
    </label><br/>
    <label>Descripción:<br/>
      <textarea name="description"><%= product.description %></textarea>
    </label><br/>
    <label>Imagen actual: <% if(product.image) { %>
      <img src="/uploads/<%= product.image %>" alt="imagen" width="100"/>
    <% } else { %>No hay imagen<% } %>
    </label><br/>
    <label>Cambiar Imagen: <input type="file" name="image" accept="image/*" /></label><br/>
    <label>Disponible: <input type="checkbox" name="isAvailable" <%= product.isAvailable ? 'checked' : '' %> /></label><br/>
    <button type="submit">Guardar Cambios</button>
  </form>
  <a href="/products">Volver a la lista</a>
</body>
</html>
